#Docker-Compose FRA

version: '3.9'
name: epo-fra
services:
  web:
    image: epo/web
    build: ./web
    container_name: web-fra
    ports:
    - 8084:8080

  rest:
    image: epo/rest
    build: ./rest
    container_name: rest-fra
    ports:
      - 8085:8080
    environment:
      - DATABASE_URL=jdbc:postgresql://psql_db:5432/postgres
      - DATABASE_USERNAME=postgres
      - DATABASE_PASSWORD=postgres
    depends_on:
      - psql_db

  psql_db:
    image: postgres
    container_name: psql_db-fra
    ports:
      - 5434:5432
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: postgres
    volumes:
      - pgdata:/var/lib/postgresql/data

  sender:
    image: epo/sender
    build: ./sender
    container_name: sender-fra
    environment:
      - spring_rabbitmq_host=rabbitmq
    ports:
      - "8086:8080"
    networks:
      - network1

  receiver:
    image: epo/receiver
    build: ./receiver
    container_name: receiver-fra
    ports:
      - "8087:8080"
    environment:
      - spring_rabbitmq_host=rabbitmq
      - QUEUE_NAME=fra
    depends_on:
      - sender    
    networks:
      - network1

networks:
  network1:
    name: rabbitmq-fra
    external: true


volumes:
  pgdata: {}
